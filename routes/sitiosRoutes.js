// ============================// /routes/sitiosRoutes.js// ============================const express = require("express");const router = express.Router();const sitiosController = require("../controllers/sitiosController");// Ruta de bienvenidarouter.get("/", sitiosController.welcome);// Listado de sitios turÃ­sticosrouter.get("/sitios", sitiosController.getSitios);// Detalle por IDrouter.get("/sitios/:id", sitiosController.getSitioById);module.exports = router;